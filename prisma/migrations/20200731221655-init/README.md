# Migration `20200731221655-init`

This migration has been generated by piann <shadow_hat@naver.com> at 7/31/2020, 10:16:55 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "bb_schema"."ProgramRuleConnBugBounty" DROP CONSTRAINT "ProgramRuleConnBugBounty_bId_fkey"

ALTER TABLE "bb_schema"."ProgramRuleConnBugBounty" DROP CONSTRAINT "ProgramRuleConnBugBounty_pId_fkey"

CREATE TABLE "bb_schema"."ProgramRuleConnBugBountyProgram" (
"id" text  NOT NULL ,
"pId" integer  NOT NULL ,
"bId" text  NOT NULL ,
"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
"updatedAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
PRIMARY KEY ("id"))

ALTER TABLE "bb_schema"."ProgramRuleConnBugBountyProgram" ADD FOREIGN KEY ("pId")REFERENCES "bb_schema"."ProgramRule"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "bb_schema"."ProgramRuleConnBugBountyProgram" ADD FOREIGN KEY ("bId")REFERENCES "bb_schema"."BugBountyProgram"("id") ON DELETE CASCADE ON UPDATE CASCADE

DROP TABLE "bb_schema"."ProgramRuleConnBugBounty";
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200731131033-init..20200731221655-init
--- datamodel.dml
+++ datamodel.dml
@@ -2,9 +2,9 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -127,9 +127,9 @@
   createdAt DateTime @default(now())
   updatedAt DateTime @updatedAt @default(now())
 }
-model ProgramRuleConnBugBounty{
+model ProgramRuleConnBugBountyProgram{
   id String @id @default(cuid())
   programRule ProgramRule @relation(fields: [pId], references: [id])
   pId Int
   bugBountyProgram BugBountyProgram @relation(fields: [bId], references: [id])
```


