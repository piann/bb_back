scalar Upload

type Query{
    hello: String
}

type Query {
  uploads: [File]
}

type File {
  filename: String!
  mimetype: String!
  encoding: String!
}

type Mutation {
  singleUpload(file: Upload!, rawData:String): File!
}